<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.junwenbu.pocketcloset"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Camera Permission -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- SDCard delete, write, read -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<!-- Internet Permission -->
	<uses-permission android:name="android.permission.INTERNET"/>
    
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.junwenbu.pocketcloset.MenuActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Camera Activity -->
        <activity
            android:name="com.junwenbu.pocketcloset.CameraActivity"
            android:label="@string/title_activity_camera" >
        </activity>

        <!-- Closet Tag View -->
        <activity
            android:name="com.junwenbu.pocketcloset.ClosetActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- Tag Activity -->
        <activity
            android:name="com.junwenbu.pocketcloset.TagActivity"
            android:label="@string/title_activity_tag" >
        </activity>

        <!-- Calendar Activity -->
        <activity
            android:name="com.junwenbu.pocketcloset.CalendarActivity"
            android:label="@string/title_activity_calendar" >
        </activity>
        
        <!-- Style -->
        <activity
            android:name="com.junwenbu.pocketcloset.StyleActivity"
            android:label="@string/title_activity_style" >
        </activity>
    </application>

</manifest>